# SPA Fallback for Render.com
# All non-file requests should serve index.html

# API routes (if any)
/api/* 200

# Static assets and files with extensions
/assets/* 200
/static/* 200
/*.js 200
/*.css 200
/*.png 200
/*.jpg 200
/*.jpeg 200
/*.gif 200
/*.svg 200
/*.ico 200
/*.woff 200
/*.woff2 200
/*.ttf 200
/*.eot 200
/*.pdf 200
/*.zip 200

# All other routes serve index.html (SPA fallback)
/*    /index.html   200
